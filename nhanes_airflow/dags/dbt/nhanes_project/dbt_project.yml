name: 'bg_nhanes_project'
version: '1.0.0'

profile: 'bg_nhanes_project'  # <--- this must match profiles.yml

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  bg_nhanes_project:
    +materialized: view